# In production the FastAPI process serves BOTH the API and this built site
# from one origin, so requests must go to relative paths (/health, /transcribe).
# api.ts uses `??` rather than `||` specifically so this empty string survives
# instead of falling back to the localhost dev default.
VITE_API_BASE=
